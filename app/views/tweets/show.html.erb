<p id="notice"><%= notice %></p>

<p>
  <h2><%= @tweet.tweet %></h2>
  <small>by: <%= @tweet.user.email %></small>
</p>

<%if @num_comments != 0%>
	we have comments! loop through them!
<%else%>
	no comments. bummer.
	<%= link_to "Add a comment", new_comment_path %>
<%end%>
